<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal sem ActionBar -->
    <style name="Theme.Mappeo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- Faz com que a barra de status não empurre o conteúdo (Edge-to-Edge) -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1" xmlns:tools="http://schemas.android.com/tools">shortEdges</item>
    </style>

    <style name="Theme.Mappeo.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1F7A52</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Mappeo</item>
    </style>
</resources>