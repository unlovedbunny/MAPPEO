package com.mappeo.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primárias ──────────────────────────────────────────────
val Green900   = Color(0xFF1B4332)
val Green800   = Color(0xFF2D6A4F)   // cor primária
val Green700   = Color(0xFF40916C)
val Green500   = Color(0xFF52B788)   // destaque
val Green200   = Color(0xFFB7E4C7)
val Green100   = Color(0xFFD8F3DC)   // fundo claro

// ── Neutras ────────────────────────────────────────────────
val White      = Color(0xFFFFFFFF)
val Black      = Color(0xFF000000)
val Gray700    = Color(0xFF374151)
val Gray500    = Color(0xFF6B7280)
val Gray200    = Color(0xFFE5E7EB)

// ── Overlay / Glassmorphism ────────────────────────────────
val OverlayDark       = Color(0xCC1A3D2B)   // 80% opacidade
val GlassBackground   = Color(0x33FFFFFF)   // 20% branco
val GlassBorder       = Color(0x4DFFFFFF)   // 30% branco
val ChipBackground    = Color(0x40FFFFFF)   // 25% branco

// ── Semânticas ─────────────────────────────────────────────
val ErrorRed   = Color(0xFFDC2626)
val SuccessGreen = Color(0xFF16A34A)

// ── Social Colors ──────────────────────────────────────────
val GoogleBlue     = Color(0xFF4285F4)
val GoogleButtonBg = Color(0xFFFFFFFF)
val AppleButtonBg  = Color(0xFF000000)

// ── Gradient Colors ────────────────────────────────────────
val SkyTop         = Color(0xFF1B4332) // Usando Green900 como base
val SkyBottom      = Color(0xFF52B788) // Usando Green500 como base

// ── Aliases para Componentes ───────────────────────────────
val ErrorColor      = ErrorRed
val PrimaryGreen    = Green800
val FieldBackground = White
val FieldBorder     = Gray200
val TextPrimary     = Gray700
val TextSecondary   = Gray500
